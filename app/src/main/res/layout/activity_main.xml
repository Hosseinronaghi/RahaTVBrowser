<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root" android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/app_background" android:orientation="vertical">

    <LinearLayout android:id="@+id/toolbar" android:layout_width="match_parent" android:layout_height="54dp"
        android:background="@color/toolbar_background" android:gravity="center_vertical" android:orientation="horizontal"
        android:paddingHorizontal="4dp" android:paddingVertical="5dp">
        <Button android:id="@+id/backButton" style="@style/ToolbarButton" android:text="‹" android:contentDescription="@string/back" />
        <Button android:id="@+id/forwardButton" style="@style/ToolbarButton" android:text="›" android:contentDescription="@string/forward" />
        <Button android:id="@+id/homeButton" style="@style/ToolbarButton" android:text="⌂" android:contentDescription="@string/home" />
        <Button android:id="@+id/reloadButton" style="@style/ToolbarButton" android:text="↻" android:contentDescription="@string/reload" />
        <EditText android:id="@+id/addressBar" android:layout_width="0dp" android:layout_height="42dp" android:layout_weight="1"
            android:layout_marginHorizontal="4dp" android:background="@drawable/address_background" android:hint="@string/address_hint"
            android:imeOptions="actionGo" android:inputType="textUri" android:maxLines="1" android:selectAllOnFocus="true"
            android:textColor="@color/text_primary" android:textColorHint="@color/text_secondary" android:textDirection="ltr" android:textSize="14sp" />
        <Button android:id="@+id/goButton" style="@style/ToolbarButton" android:text="→" android:contentDescription="@string/go" />
        <Button android:id="@+id/favoriteButton" style="@style/ToolbarButton" android:text="☆" android:contentDescription="@string/add_favorite" />
        <Button android:id="@+id/playerButton" style="@style/ToolbarButton" android:text="▶" android:contentDescription="@string/open_player" />
        <Button android:id="@+id/desktopButton" style="@style/ToolbarButton" android:text="D" android:contentDescription="@string/desktop_mode" />
        <Button android:id="@+id/modeButton" style="@style/ToolbarButton" android:text="◎" android:contentDescription="@string/cursor_mode" />
        <Button android:id="@+id/tabsButton" style="@style/ToolbarButton" android:text="1" android:contentDescription="@string/tabs" />
        <Button android:id="@+id/privateButton" style="@style/ToolbarButton" android:text="◉" android:contentDescription="@string/new_private_tab" />
        <Button android:id="@+id/settingsButton" style="@style/ToolbarButton" android:text="⚙" android:contentDescription="@string/settings" />
        <Button android:id="@+id/closeButton" style="@style/ToolbarButton" android:text="×" android:contentDescription="@string/close_tab" />
    </LinearLayout>

    <ProgressBar android:id="@+id/progressBar" style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent" android:layout_height="2dp" android:max="100"
        android:progressTint="@color/accent" android:visibility="gone" />

    <FrameLayout android:id="@+id/browserFrame" android:layout_width="match_parent" android:layout_height="0dp"
        android:layout_weight="1" android:focusable="true" android:focusableInTouchMode="true">
        <org.mozilla.geckoview.GeckoView android:id="@+id/geckoView" android:layout_width="match_parent"
            android:layout_height="match_parent" android:background="#FFFFFF" android:keepScreenOn="true" />
        <com.raha.browser.tv.CursorOverlayView android:id="@+id/cursorOverlay" android:layout_width="match_parent"
            android:layout_height="match_parent" android:clickable="false" android:elevation="100dp"
            android:focusable="true" android:focusableInTouchMode="true" android:visibility="gone" />
    </FrameLayout>
</LinearLayout>
