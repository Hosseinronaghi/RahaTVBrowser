# RahaTVBrowser 0.3.1

- رفع خطای Build ناشی از استفاده هم‌زمان از `ndk.abiFilters` و `splits.abi`.
- حفظ APKهای جداگانه `arm64-v8a` و `armeabi-v7a` از طریق ABI splits.
- حفظ R8، Resource Shrinking و خروجی AAB بهینه برای Google Play.
- افزایش `versionCode` به 8 و `versionName` به 0.3.1.
